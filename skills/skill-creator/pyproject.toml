[project]
name = "skill-creator"
version = "0.0.0"
description = "Tooling for authoring, evaluating, and benchmarking skills."
requires-python = ">=3.10"
dependencies = ["pyyaml>=6"]

[tool.uv]
package = false
